import std.conv
import std.random


debug
    @property string seed()
        return uniform!uint().to!string
else
    shared immutable string seed

    shared static this()
        seed = uniform!uint().to!string
