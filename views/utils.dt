- import site_utils;

- void script(string src) @safe
  script(type="text/javascript", src="#{prefix}/static/#{src}.js?a=#{seed}")

- void stylesheet(string href) @safe
  link(rel="stylesheet", type="text/css", href="#{prefix}/static/#{href}.css?a=#{seed}")
